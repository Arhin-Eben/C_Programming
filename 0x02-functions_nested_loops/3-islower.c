#include "main.h"

/**
 * main - check the code.
 *Description: this have been added to the file
 *@parameter: it's returning A number
 * Return: Always 0.
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}

	return (0);
}	
